# Migration records

`ledger.json` is the complete, machine-readable account of the reconstruction. It retains volatile and detailed historical bookkeeping. Live nodes retain only stable legacy path, heading, and revision pointers.

Every file tracked at the baseline revision must appear exactly once in the ledger, including files that remain at the repository root.
