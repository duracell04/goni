# Artifacts

Non-narrative schemas, APIs, diagrams, datasets, measurements, manifests, and media live here. Every artifact must be owned by at least one canonical node through that node's `artifacts` metadata.
