# Sources

Source records identify external or internal material without converting it into GONI doctrine. Each YAML record validates against `schema/source.schema.json`. Evidence nodes must distinguish what a source reports from GONI's interpretation of it.
