# Tooling

Repository tooling migrates the legacy corpus, builds deterministic projections, and validates the graph. Generated content must not depend on clocks, locale, filesystem enumeration order, or mutable remote references.
