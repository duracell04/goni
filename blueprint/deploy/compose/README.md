# Compose

Compose manifests are specified only and not yet checked in.
This folder is reserved for overlays (for example, observability) once a canonical compose file lands.

See `/blueprint/docs/STATUS.md` for current implementation status.
