# GONI ontology

`relations.yml` is the authoritative relation vocabulary. Authors write only the forward edge that best represents their claim. Tooling generates reverse projections; reverse edges are never hand-maintained.

Folders encode node type. Metadata carries domain, draft status, implementation state, aliases, relations, source IDs, uncertainty, artifacts, and stable legacy pointers.
